export enum SIZES {
  default = 'default',
  small = 'small',
  smallExtra = 'smallExtra',
  medium = 'medium',
  base = 'base',
  large = 'large',
  largeExtra = 'largeExtra',
  full = 'full'
}

export enum VARIANTS {
  default = 'default',
  medium = 'medium',
  status = 'status',
  transparent = 'transparent',
  uppercase = 'uppercase',
  noBorder = 'noBorder',
  muted = 'muted',
  action = 'action',
  icon = 'icon',
  selected = 'selected'
}
