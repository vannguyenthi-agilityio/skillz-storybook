export enum SIZES {
  default = 'default',
  xs = 'xs',
  sm = 'sm',
  md = 'md',
  base = 'base',
  lg = 'lg',
  lx = 'lx',
  full = 'full'
}

export enum VARIANTS {
  default = 'default',
  medium = 'medium',
  status = 'status',
  transparent = 'transparent',
  uppercase = 'uppercase',
  noBorder = 'noBorder',
  muted = 'muted',
  action = 'action',
  icon = 'icon',
  selected = 'selected'
}

export enum ICON_POSITION {
  left = 'left',
  right = 'right'
}
